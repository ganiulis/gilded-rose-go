# GO Starter

- Build:

```shell
make build
```

- Run tests:

```shell
make test
```

- Run go inside container

```shell
make go ...
```

- Run app:

```shell
make run [<number-of-days>; default: 2]
```

- Run tests with coverage output:

```shell
make coverage
```
