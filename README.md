# GO Starter

- Build:

```shell
make build
```

- Run tests:

```shell
make test
```

- Run a command inside container

```shell
make cmd args="..."
```

- Run app:

```shell
make run [days=...; default: 2]
```

- Run tests with coverage output:

```shell
make coverage
```
